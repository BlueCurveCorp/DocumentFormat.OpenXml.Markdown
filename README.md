# DocumentFormat.OpenXml.Markdown
A lightweight extension for the official Open XML SDK that reliably converts Word (.docx), Excel (.xlsx), and PowerPoint (.pptx) documents into clean, GitHub-Flavored Markdown.
